<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title> Node JS | Simple Todo List  </title>
    <style type="text/css">
        *{
            margin:0;
            padding:0;
            box-sizing:border-box;
        }
        body{
            min-height:100vh;
            background:#ff3;
            color:#000;
            display:flex;
            flex-direction:column;
            justify-content:center;
            align-items:center;
            flex-wrap:wrap;
        }
        body ul{
            margin:20px;
        }
        form
        {
            width:70%;
            display:flex;
            flex-direction:column;
            justify-content:center;
            align-items:center;
            flex-wrap:wrap;
            background:transparent;
        }
        form input
        {
            width:60%;
            outline:none;
            border:none;
            border-bottom:3px solid blue;
            color:#000;
            background:transparent;

        }
        form button{
            width:50px;
            height:40px;
            color:#000;
        }
        form button:hover{
            cursor:pointer;
            background:#333;
            border-radius:5px;
            color:#fff;
            outline:none;
            border:none;
        }
    </style>
</head>
<body>
    <h1> Project 2: Simple Todo List App</h1>
    <ul>
        <% 
        tasks.forEach((task,index)=>{ %>
            <li> <%= task %> </li>

       <% })
        %>
    </ul>
    <form action="/add" method="POST">
        <input type="text" name="task" placeholder="Enter your task here " />
        <br/>
        <button type="submit"> Add Task </button>

    </form>
</body>
</html>